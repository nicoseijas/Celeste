<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:controls="clr-namespace:Celeste.Wpf.Controls">

    <!--
        Placeholder text is a TextBlock behind the content host, hidden by a trigger on Text="".
        That keeps it out of the logical tree of the input, so it never lands in Text, in the
        clipboard, or in a binding.
    -->
    <ControlTemplate x:Key="Celeste.Template.TextBox" TargetType="{x:Type TextBox}">
        <Grid>
            <Border x:Name="FocusRing"
                    Margin="-3"
                    BorderBrush="{DynamicResource Celeste.Brush.Ring}"
                    BorderThickness="2"
                    CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:ControlHelper.CornerRadius)}"
                    Opacity="0"
                    SnapsToDevicePixels="True" />

            <Border x:Name="Root"
                    Background="{TemplateBinding Background}"
                    BorderBrush="{TemplateBinding BorderBrush}"
                    BorderThickness="{TemplateBinding BorderThickness}"
                    CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:ControlHelper.CornerRadius)}"
                    SnapsToDevicePixels="True">
                <Grid>
                    <TextBlock x:Name="Placeholder"
                               Margin="{TemplateBinding Padding}"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource Celeste.Brush.MutedForeground}"
                               IsHitTestVisible="False"
                               Text="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:ControlHelper.PlaceholderText)}"
                               Visibility="Collapsed" />
                    <ScrollViewer x:Name="PART_ContentHost"
                                  Padding="{TemplateBinding Padding}"
                                  VerticalAlignment="Center"
                                  Focusable="False"
                                  HorizontalScrollBarVisibility="Hidden"
                                  VerticalScrollBarVisibility="Hidden" />
                </Grid>
            </Border>
        </Grid>

        <ControlTemplate.Triggers>
            <Trigger Property="Text" Value="">
                <Setter TargetName="Placeholder" Property="Visibility" Value="Visible" />
            </Trigger>
            <Trigger Property="IsKeyboardFocusWithin" Value="True">
                <Setter TargetName="FocusRing" Property="Opacity" Value="1" />
                <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Celeste.Brush.Ring}" />
            </Trigger>
        </ControlTemplate.Triggers>
    </ControlTemplate>

    <Style x:Key="Celeste.TextBox" TargetType="{x:Type TextBox}">
        <Setter Property="controls:ControlHelper.CornerRadius" Value="{DynamicResource Celeste.Radius.Md}" />
        <Setter Property="Background" Value="{DynamicResource Celeste.Brush.Surface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Celeste.Brush.Input}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{DynamicResource Celeste.Brush.Foreground}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontFamily" Value="{DynamicResource Celeste.FontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource Celeste.FontSize.Body}" />
        <Setter Property="Foreground" Value="{DynamicResource Celeste.Brush.Foreground}" />
        <Setter Property="MinHeight" Value="{DynamicResource Celeste.Control.HeightMd}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Celeste.Brush.Primary}" />
        <Setter Property="SelectionOpacity" Value="0.35" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template" Value="{StaticResource Celeste.Template.TextBox}" />
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Celeste.Brush.InputHover}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource Celeste.Brush.Disabled}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Celeste.Brush.DisabledBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Celeste.Brush.DisabledForeground}" />
            </Trigger>
            <Trigger Property="IsReadOnly" Value="True">
                <Setter Property="Background" Value="{DynamicResource Celeste.Brush.Muted}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource Celeste.TextBox}" TargetType="{x:Type TextBox}" />

    <Style x:Key="Celeste.TextBox.Multiline"
           BasedOn="{StaticResource Celeste.TextBox}"
           TargetType="{x:Type TextBox}">
        <Setter Property="AcceptsReturn" Value="True" />
        <Setter Property="MinHeight" Value="96" />
        <Setter Property="TextWrapping" Value="Wrap" />
        <Setter Property="VerticalContentAlignment" Value="Top" />
    </Style>

    <Style x:Key="Celeste.PasswordBox" TargetType="{x:Type PasswordBox}">
        <Setter Property="controls:ControlHelper.CornerRadius" Value="{DynamicResource Celeste.Radius.Md}" />
        <Setter Property="Background" Value="{DynamicResource Celeste.Brush.Surface}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Celeste.Brush.Input}" />
        <Setter Property="BorderThickness" Value="1" />
        <Setter Property="CaretBrush" Value="{DynamicResource Celeste.Brush.Foreground}" />
        <Setter Property="FocusVisualStyle" Value="{x:Null}" />
        <Setter Property="FontFamily" Value="{DynamicResource Celeste.FontFamily}" />
        <Setter Property="FontSize" Value="{DynamicResource Celeste.FontSize.Body}" />
        <Setter Property="Foreground" Value="{DynamicResource Celeste.Brush.Foreground}" />
        <Setter Property="MinHeight" Value="{DynamicResource Celeste.Control.HeightMd}" />
        <Setter Property="Padding" Value="10,6" />
        <Setter Property="SelectionBrush" Value="{DynamicResource Celeste.Brush.Primary}" />
        <Setter Property="SnapsToDevicePixels" Value="True" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type PasswordBox}">
                    <Grid>
                        <Border x:Name="FocusRing"
                                Margin="-3"
                                BorderBrush="{DynamicResource Celeste.Brush.Ring}"
                                BorderThickness="2"
                                CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:ControlHelper.CornerRadius)}"
                                Opacity="0"
                                SnapsToDevicePixels="True" />
                        <Border x:Name="Root"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{Binding RelativeSource={RelativeSource TemplatedParent}, Path=(controls:ControlHelper.CornerRadius)}"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Padding="{TemplateBinding Padding}"
                                          VerticalAlignment="Center"
                                          Focusable="False"
                                          HorizontalScrollBarVisibility="Hidden"
                                          VerticalScrollBarVisibility="Hidden" />
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="FocusRing" Property="Opacity" Value="1" />
                            <Setter TargetName="Root" Property="BorderBrush" Value="{DynamicResource Celeste.Brush.Ring}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="BorderBrush" Value="{DynamicResource Celeste.Brush.InputHover}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Background" Value="{DynamicResource Celeste.Brush.Disabled}" />
                <Setter Property="BorderBrush" Value="{DynamicResource Celeste.Brush.DisabledBorder}" />
                <Setter Property="Foreground" Value="{DynamicResource Celeste.Brush.DisabledForeground}" />
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style BasedOn="{StaticResource Celeste.PasswordBox}" TargetType="{x:Type PasswordBox}" />

</ResourceDictionary>
